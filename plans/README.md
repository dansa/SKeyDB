# Implementation Plans

Generated by the improve skill on 2026-06-18 for the Builder V2 audit. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Cap import payloads before decode | P1 | S | none | DONE |
| 002 | Put Builder V2 in the integration gate | P1 | S | none | DONE |
| 003 | Add Builder V2 browser smoke verification | P1 | M | none | DONE |
| 004 | Reconcile Builder V2 beta promotion truth | P1 | S-M | none | DONE |
| 005 | Normalize mobile quick-lineup density safely | P1 | M | 003 recommended | DONE |
| 006 | Narrow the Builder V2 DnD command port | P2 | M | none | DONE |
| 007 | Window Builder V2 picker result grids | P2 | M | 003 recommended | DONE |
| 008 | Split the Builder V2 model hook by ownership | P2 | L | 006 | TODO |
| 008a | Extract Builder V2 persisted preferences | P2 | S-M | 006 | DONE |
| 008b | Extract Builder V2 team-management commands | P2 | M | 006, 008a | DONE |
| 008c | Extract Builder V2 import/export adapter | P2 | S-M | 008a, 008b | DONE |
| 009 | Prepare Builder V2 visual code for the design pass | P2 | M-L | 003 recommended | TODO |
| 010 | Investigate lazy Builder asset URL maps | P3 | M | none | REJECTED: sync API keeps eager URL maps; async boundary out of scope |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency Notes

- 003 should land before 005, 007, or 009 when possible because those plans touch
  browser-visible layout, DnD, or responsive styling.
- 006 should land before 008 because it removes one narrow consumer from the
  oversized `BuilderV2Model` before the broader model-hook split.
- 005 may proceed before 003 only if the executor records manual desktop,
  adaptive, and 390px mobile screenshots or DOM measurements in its final
  receipt. The layout is tight, so this cannot be a blind CSS bump.
- 009 can be either a pure selector/file split or a picker primitive extraction
  tranche. Do not combine the whole CSS split, picker extraction, and mobile
  density redesign in one implementation.

## Verification Baseline

Known-good read-only checks at generation time:

- `npx tsc -p tsconfig.app.json --noEmit` passed.
- `npm run lint` passed.
- `npx vitest run src/features/builder-v2/builder-v2-dnd.test.ts src/features/builder-v2/useBuilderV2Model.test.ts src/features/builder-v2/BuilderV2Page.test.tsx --run` passed, 146 tests.
- `npm audit --omit=dev --json` reported zero production vulnerabilities.

Common repo gates:

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| Typecheck | `npx tsc -p tsconfig.app.json --noEmit` | exit 0, no output |
| Lint | `npm run lint` | exit 0 |
| Builder V2 focused tests | `npx vitest run src/features/builder-v2/builder-v2-dnd.test.ts src/features/builder-v2/useBuilderV2Model.test.ts src/features/builder-v2/BuilderV2Page.test.tsx --run` | all tests pass |
| Full bounded tests | `npm run test:bounded` | all tests pass |
| Production build | `npm run build:quiet` | exit 0 |
| Format check | `npm run format:check` | exit 0 |

## Findings Considered And Rejected

- Core Builder V2 DnD/model correctness bug: rejected. A read-only correctness
  pass over the primary DnD/model paths found no current bug, and the focused
  Builder V2 suite passed.
- Builder V2 default-route opt-in test gap: rejected. `src/App.test.tsx` already
  covers opting into Builder V2 as default, redirecting `/builder` to
  `/builder-v2`, and preserving `/builder?classic=1`.
- Production dependency vulnerability: rejected. `npm audit --omit=dev --json`
  reported zero production vulnerabilities at generation time.
